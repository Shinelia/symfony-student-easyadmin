{% extends 'base.html.twig' %}

{% block title %}Bienvenue sur notre application{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>
    <p> Voici la liste des promos : 
    {# <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>StartDate</th>
                <th>DateEnd</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for school_year in school_years %}
            <tr>
                <td>{{ school_year.id }}</td>
                <td>{{ school_year.name }}</td>
                <td>{{ school_year.startDate ? school_year.startDate|date('Y-m-d') : '' }}</td>
                <td>{{ school_year.dateEnd ? school_year.dateEnd|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('school_year_show', {'id': school_year.id}) }}">show</a>
                    <a href="{{ path('school_year_edit', {'id': school_year.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
     #}
</div>
{% endblock %}
